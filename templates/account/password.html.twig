{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe - La Boutique Française{% endblock %}

{% block content %}
    <h1>Modifier mon mot de passe</h1>
    <a href="{{ path('account')}}">Retourner sur mon compte</a><br>

    {% if notification %}
        <div class="alert alert-{{ notification.class }}">{{ notification.message }}</div>
    {% endif %}

    {{ form(form) }}


{% endblock %}
